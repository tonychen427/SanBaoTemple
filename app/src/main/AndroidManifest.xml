<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.foguangshan.sanbaotemple" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name="._core.AppController"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:icon="@drawable/ic_launcher"
        >
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".MyPageActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".MyUIPageView"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".PullRefreshGridActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".TabSliderActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ParallexActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".DragPanelActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>
